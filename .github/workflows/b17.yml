name: Build and Test

on: workflow_dispatch


jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: code checkout 
        run: |
         git clone https://github.com/ExlearnTechnology/GA.git
         cd GA
         ls -l
      